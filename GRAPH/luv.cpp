// boiler plate for cpp
#include <iostream>
#include <vector>

using namespace std;

// dfs in tree
void DFSINTREE(int vertex, int par)
{

    // take action on the vertex

    // go to all the child of the vertex
    // for(int child : maxtrix[verx]){
    //     // take action on child node

    //     if(child==par) continue;
    //     DFSINTREE(child,vertex);

    // }
}

int main()
{
    cout << "Hello World!";
    return 0;
}