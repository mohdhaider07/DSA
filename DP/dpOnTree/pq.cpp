#include<iostream>
#include<map>
using namespace std;
/*
//  map<TreeNode*,int>mp;
//     map<TreeNode*,int>depth;
    int depthOfTree(TreeNode * root){
        if(!root) return 0;
        int left=depth.find(root->left)!=depth.end()? depth[root->left]:
                     depth[root->left]=depthOfTree(root->left);

         int right=depth.find(root->right)!=depth.end()? depth[root->right]:
                     depth[root->right]=depthOfTree(root->right);

        return max(left,right)+1;


    }

    void helper(TreeNode * root,int &ans){
        if(!root){
            return;
        }

        int left=  mp.find(root->left)!=mp.end() ? mp[root->left] : 
                                mp[root->left] = depthOfTree(root->left);


        int right=  mp.find(root->right)!=mp.end() ? mp[root->right] : 
                                mp[root->right] = depthOfTree(root->right);

        ans=max(left+right,ans);

        helper(root->left,ans);
        helper(root->right,ans);
        


    }
     int diameterOfBinaryTree(TreeNode* root) {
     int ans=INT_MIN;
     helper(root,ans);
     return ans;

    }
*/
 int main(){
    cout<<"Haider";
    return 0;
 }   